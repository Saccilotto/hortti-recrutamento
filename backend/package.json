{
  "name": "hortti-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "nest start",
    "start:dev": "nest start --watch",
    "test": "jest --watchAll=false --runInBand"
  }
}